function solve(inpt) {
    inpt = inpt.split(' ');
    let object = [];

    for (let word of inpt) {
        object.push(word);
    }
    console.log(object);
}
solve('Java C# Php PHP Java PhP 3 C# 3 1 5 C#')